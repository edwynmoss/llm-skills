# Fixture scope

This supplied directory is the entire fictional project; no Git repository or deployed system is provided. Read README.md for its contract. Use existing functions and standard-library SQLite. Keep changes in this directory. Tests are authorized to write only to their in-memory database; no network, dependencies, external databases or server processes. Do not stage or commit. The evaluating parent owns final fixture-directory cleanup.
